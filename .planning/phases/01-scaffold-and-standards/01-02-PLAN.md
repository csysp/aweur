---
phase: 01-scaffold-and-standards
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - LICENSE
  - .markdownlint-cli2.jsonc
  - README.md
autonomous: true
requirements:
  - STRC-03
  - QUAL-03

must_haves:
  truths:
    - "LICENSE file exists in the repository root with CC0-1.0 text"
    - "GitHub auto-detects the repo license as CC0-1.0"
    - "Inclusion criteria section is present in README.md, covering all four policy areas from D-08 through D-11"
    - ".markdownlint-cli2.jsonc exists and disables conflicting rules (MD013, MD033, MD034, MD041)"
    - "Both `npx awesome-lint` and `npx markdownlint-cli2 README.md` exit 0"
  artifacts:
    - path: "LICENSE"
      provides: "CC0-1.0 public domain dedication"
      contains: "CC0 1.0 Universal"
    - path: ".markdownlint-cli2.jsonc"
      provides: "markdownlint configuration silencing awesome-list-incompatible rules"
    - path: "README.md"
      provides: "Inclusion criteria prose block between intro and ## Contents"
      contains: "open source"
  key_links:
    - from: "README.md inclusion criteria"
      to: "QUAL-03 requirement"
      via: "prose defining open source threshold, Eurorack scope, archived policy, partially open policy"
      pattern: "public GitHub"
---

<objective>
Create the LICENSE file with canonical CC0-1.0 text, add the markdownlint config that suppresses rules incompatible with awesome-list conventions, insert an inclusion criteria prose block into README.md, and run both linters to confirm the complete Phase 1 scaffold passes with zero errors.

Purpose: Satisfy the remaining Phase 1 requirements (STRC-03 LICENSE, QUAL-03 inclusion criteria) and lock in the quality gate before Phase 2 content work begins.

Output: LICENSE, .markdownlint-cli2.jsonc, and updated README.md — all passing the full two-linter suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-and-standards/01-CONTEXT.md
@.planning/phases/01-scaffold-and-standards/01-RESEARCH.md
@.planning/phases/01-scaffold-and-standards/01-01-SUMMARY.md

<interfaces>
<!-- Locked inclusion criteria policies from CONTEXT.md -->

D-08: Open source threshold — any public GitHub repo with accessible source files. No OSI license required, but source files must be publicly viewable.

D-09: Partially open projects — include if hardware files (PCB, schematic) are publicly available, even if firmware/software is proprietary. Tag with only applicable type tags (e.g., `HW` only if firmware is closed).

D-10: Archived/unmaintained repos — include with `[Archived]` label in description.

D-11: Eurorack scope — module must be specifically designed for Eurorack: 3U (128.5mm) panel height, ±12V power via Doepfer-compatible header, 3.5mm mono patch jacks. Multi-format projects excluded unless Eurorack is primary/documented target.

Inclusion criteria placement decision (Claude's discretion per CONTEXT.md):
  - Place as inline prose BETWEEN the intro paragraph/badge and the ## Contents section
  - No ## heading required (keeps it out of TOC, reduces TOC clutter for a scaffold-only phase)
  - If awesome-lint requires TOC completeness for any named heading, either omit the heading or add it to TOC
  - Reference: RESEARCH.md "Pattern 3: Inclusion Criteria Section" — safest is inline prose before ## Contents

LICENSE source (RESEARCH.md Pattern 4):
  - Canonical CC0-1.0 text from Creative Commons
  - File named exactly `LICENSE` (no extension)
  - GitHub auto-detects if the canonical text is present

markdownlint config (RESEARCH.md Standard Stack):
  - File: .markdownlint-cli2.jsonc
  - Disable: MD013 (line length), MD033 (inline HTML for badges), MD034 (bare URLs), MD041 (first line heading)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE file and .markdownlint-cli2.jsonc</name>
  <files>LICENSE, .markdownlint-cli2.jsonc</files>

  <read_first>
    - .planning/phases/01-scaffold-and-standards/01-RESEARCH.md (Pattern 4: CC0 LICENSE file section; Standard Stack markdownlint config)
    - .planning/phases/01-scaffold-and-standards/01-01-SUMMARY.md (confirm README.md lint-passing state before adding files)
  </read_first>

  <action>
Create two files:

FILE 1: LICENSE (no extension, in project root C:\Users\MJMC\Desktop\aweur\)

Fetch the canonical CC0-1.0 text. Use WebFetch on:
  https://choosealicense.com/licenses/cc0-1.0/

Extract just the license body text (the legal prose, not the HTML page). The file should begin with the Creative Commons legal deed text. The key identifiers GitHub uses to auto-detect CC0-1.0 are:
  - "CC0 1.0 Universal" in the text
  - "Creative Commons Corporation" in the text
  - "publicdomain/zero/1.0" URL reference

If WebFetch is unavailable, use this authoritative minimal CC0 header followed by the standard legal text. The canonical text opens with:

```
Creative Commons Legal Code

CC0 1.0 Universal

    CREATIVE COMMONS CORPORATION IS NOT A LAW FIRM AND DOES NOT PROVIDE
    LEGAL SERVICES. DISTRIBUTION OF THIS DOCUMENT DOES NOT CREATE AN
    ATTORNEY-CLIENT RELATIONSHIP. CREATIVE COMMONS PROVIDES THIS
    INFORMATION ON AN "AS-IS" BASIS. CREATIVE COMMONS MAKES NO WARRANTIES
    REGARDING THE USE OF THIS DOCUMENT OR THE INFORMATION OR WORKS
    PROVIDED HEREUNDER, AND DISCLAIMS LIABILITY FOR DAMAGES RESULTING FROM
    THE USE OF THIS DOCUMENT OR THE INFORMATION OR WORKS PROVIDED
    HEREUNDER.
```

The full CC0-1.0 text is approximately 400 words. Write the complete text — do not truncate. The complete text is required for GitHub license auto-detection.

FILE 2: .markdownlint-cli2.jsonc (in project root)

Write exactly:

```jsonc
{
  "config": {
    "MD013": false,
    "MD033": false,
    "MD034": false,
    "MD041": false
  }
}
```

MD013: line length — awesome-list entries are naturally long
MD033: inline HTML — needed for badge rendering
MD034: bare URLs — sometimes used in resource sections
MD041: first line heading — awesome-lint handles H1 validation, markdownlint would conflict
  </action>

  <verify>
    <automated>test -f LICENSE && grep -l "CC0" LICENSE && test -f .markdownlint-cli2.jsonc && echo "OK: both files exist"</automated>
  </verify>

  <acceptance_criteria>
    - File `LICENSE` exists in the project root (C:\Users\MJMC\Desktop\aweur\LICENSE)
    - `LICENSE` contains the string "CC0 1.0 Universal"
    - `LICENSE` contains the string "Creative Commons"
    - `LICENSE` contains the string "publicdomain/zero/1.0" or "public domain"
    - `LICENSE` is at least 200 lines or 3000 characters (full legal text, not truncated)
    - File `.markdownlint-cli2.jsonc` exists in the project root
    - `.markdownlint-cli2.jsonc` contains `"MD013": false`
    - `.markdownlint-cli2.jsonc` contains `"MD033": false`
    - `.markdownlint-cli2.jsonc` contains `"MD041": false`
    - `npx awesome-lint` still exits 0 after these files are added (LICENSE presence satisfies awesome-lint's license check)
  </acceptance_criteria>

  <done>
    LICENSE file exists with complete CC0-1.0 text. .markdownlint-cli2.jsonc exists with the four disabled rules. `npx awesome-lint` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inclusion criteria prose to README.md and run full lint gate</name>
  <files>README.md</files>

  <read_first>
    - README.md (read current state — must see exactly where badge ends and ## Contents begins before inserting prose)
    - .planning/phases/01-scaffold-and-standards/01-CONTEXT.md (D-08, D-09, D-10, D-11 — the four policy areas to document; also inclusion criteria section example text in RESEARCH.md Code Examples)
    - .planning/phases/01-scaffold-and-standards/01-RESEARCH.md (Code Examples: "Inclusion Criteria section text" — use as the basis, adjust wording freely)
  </read_first>

  <action>
Insert an inclusion criteria prose block into README.md. Placement: immediately before `## Contents` (after the Awesome badge line and any trailing blank line).

The prose must cover all four locked policies (D-08 through D-11). Do NOT use a `##` heading for this block — keep it as plain prose to avoid adding a TOC entry for a scaffold-only phase. If testing reveals that awesome-lint requires all prose to be in sections, add `## Inclusion Criteria` and add it to the TOC.

Write the prose block as follows (exact wording is Claude's discretion — this is a starting point):

```
**What qualifies as open source:** Any public GitHub repository with accessible source
files — hardware design files (KiCad/Eagle schematics and PCB layouts), firmware source
code, or software source code. A formal OSI-approved license is not required, but source
files must be publicly viewable. Projects with only compiled binaries or documentation
do not qualify.

**What qualifies as Eurorack:** The module or project must be specifically designed for
the Eurorack format: 3U (128.5mm) panel height, power via ±12V Doepfer-compatible
header, 3.5mm mono patch jacks. Multi-format projects are included only if Eurorack is
the primary or explicitly documented target format.

**Archived and unmaintained projects:** Included with an `[Archived]` label in the
description. Hardware designs age well; firmware may have dependency rot, but the
design remains valuable.

**Partially open projects:** Included if hardware files (schematics, PCB) are publicly
available, even if firmware or software is proprietary. Tagged with only the applicable
type tags (e.g., `` `HW` `` only if firmware is closed).
```

After inserting this block, run the full lint suite:

  npx awesome-lint
  npx markdownlint-cli2 README.md

Fix any errors from either linter before completing this task.

Common markdownlint-cli2 issues to watch for:
  - Blank lines around headings (MD022) — ensure blank line before and after every ## heading
  - Ordered lists (MD029) — not applicable here (no numbered lists)
  - Hard tabs (MD010) — use spaces only
  - Trailing whitespace (MD009) — no trailing spaces on any line

If `npx awesome-lint` reports any issue with the prose block (e.g., unexpected content before ## Contents), convert the block to a named `## Inclusion Criteria` section, add it to the TOC (before the existing 14 entries), run doctoc again (`npx doctoc README.md --github`), and re-run both linters.

Do NOT complete this task until BOTH linters exit 0.
  </action>

  <verify>
    <automated>npx awesome-lint && npx markdownlint-cli2 README.md && echo "PHASE 1 QUALITY GATE: PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - `npx awesome-lint` exits 0
    - `npx markdownlint-cli2 README.md` exits 0
    - README.md contains the string "public GitHub" (open source threshold policy, D-08)
    - README.md contains the string "3U" (Eurorack format spec, D-11)
    - README.md contains the string "±12V" or "+/-12V" (power spec, D-11)
    - README.md contains the string "3.5mm" (jack spec, D-11)
    - README.md contains the string "[Archived]" in an example or policy description (D-10)
    - README.md contains policy text covering partially open projects (D-09) — grep for "partially" or "hardware files"
    - Both linters pass with zero errors — confirmed by running `npx awesome-lint && npx markdownlint-cli2 README.md` and observing exit code 0
  </acceptance_criteria>

  <done>
    README.md contains inclusion criteria prose covering all four locked policies (D-08 through D-11). Both `npx awesome-lint` and `npx markdownlint-cli2 README.md` exit 0. Phase 1 quality gate is passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebFetch for LICENSE text | Fetching CC0 text from choosealicense.com — only read, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | LICENSE content | accept | File is static Markdown/text. Low risk. Verify CC0 text matches canonical source. |
| T-02-02 | Information Disclosure | .markdownlint-cli2.jsonc | accept | Config file disables lint rules only. No secrets, no credentials, no sensitive data. |
</threat_model>

<verification>
Full Phase 1 completion check after both plans are done:

```bash
# Primary gates
npx awesome-lint
npx markdownlint-cli2 README.md

# STRC-01: lint compliance
echo "STRC-01:" && npx awesome-lint 2>&1 | tail -3

# STRC-02: TOC completeness — 14 category anchors
grep -c "(#" README.md

# STRC-03: LICENSE exists with CC0 text
grep "CC0 1.0" LICENSE

# STRC-04/STRC-05: example entry format with tags
grep -E "\`HW\`|\`FW\`|\`SW\`" README.md | head -5

# STRC-06: alphabetical (with one entry per section, trivially satisfied)
echo "STRC-06: trivially satisfied with one entry per section"

# STRC-07: intro paragraph
head -15 README.md

# QUAL-03: inclusion criteria
grep "public GitHub" README.md
grep "3.5mm" README.md
grep "Archived" README.md
```
</verification>

<success_criteria>
- LICENSE contains complete CC0-1.0 text and GitHub auto-detects it as CC0-1.0
- .markdownlint-cli2.jsonc exists with MD013, MD033, MD034, MD041 disabled
- README.md contains inclusion criteria prose covering all four policies: open source threshold (D-08), partially open projects (D-09), archived repos (D-10), Eurorack scope (D-11)
- `npx awesome-lint` exits 0
- `npx markdownlint-cli2 README.md` exits 0
- All 8 Phase 1 requirements (STRC-01 through STRC-07, QUAL-03) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-and-standards/01-02-SUMMARY.md` including:
- Confirmation that LICENSE was created with complete CC0-1.0 text
- Confirmation that .markdownlint-cli2.jsonc was created
- Confirmation that inclusion criteria prose was inserted into README.md
- Full output of `npx awesome-lint && npx markdownlint-cli2 README.md` (both must show clean)
- Summary of all 8 Phase 1 requirement IDs and their satisfied state
</output>
